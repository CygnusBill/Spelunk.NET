# Statement ID Implementation Plan

## Current State
- Statement IDs generated by find-statements (`stmt-1`, `stmt-2`, etc.)
- No persistence - IDs exist only during search call
- replace-statement accepts statementId parameter but returns "not implemented"

## Implementation Plan
1. Add session-scoped statement tracking to RoslynWorkspaceManager
2. Store statement ID → SyntaxNode mappings when find-statements runs
3. Implement lookup mechanism for statement operations
4. Update replace-statement to use statement IDs
5. Test the complete workflow

## Design Decisions
- Use Dictionary<string, SyntaxNode> for statement tracking
- Session-scoped (not persisted to disk)
- Clear statement cache when workspace reloads
- Statement IDs complement markers (immediate vs edit-resilient)

## Started: User left, proceeding with implementation